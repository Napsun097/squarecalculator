# 🔢 Parallel Square Calculator (with User Input)

โปรเจกต์นี้ใช้ `multiprocessing` เพื่อคำนวณเลขยกกำลังสองแบบขนาน โดยรับค่าตัวเลขจากผู้ใช้

## 🚀 วิธีใช้งาน
1. **ติดตั้ง Python** (รองรับ Python 3 ขึ้นไป)
2. **รันโปรแกรม**
   ```bash
   python square_calculator.py
